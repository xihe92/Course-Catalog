<p id="notice"><%= notice %></p>

<h1>Listing Courses</h1>

<table>
  <thead>
    <tr>
      <th>Course code</th>
      <th>Course name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td align="center"><%= course.c_code %></td>
        <td align="center"><%= course.c_name %></td>
        <td align="center"><%= link_to 'Show', course %></td>
        <td align="center"><%= link_to 'Edit', edit_course_path(course) %></td>
        <td align="center"><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Course', new_course_path %>
